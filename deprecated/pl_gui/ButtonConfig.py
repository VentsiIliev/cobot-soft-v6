class ButtonConfig:
    def __init__(self, normalIconPath, pressedIconPath, tooltip, callback):
        self.normalIconPath = normalIconPath
        self.pressedIconPath = pressedIconPath
        self.tooltip = tooltip
        self.callback = callback